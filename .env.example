# Firebase web API key (public identifier, but kept out of the repo to avoid scraping).
# Copy this file to .env.local and fill the value for local dev.
# In CI, set it as the GitHub Actions secret VITE_FIREBASE_API_KEY.
VITE_FIREBASE_API_KEY=
